- if current_user.notifications.present?
  %h2 Notifications
  %ul
    - current_user.notifications.each do |notification|
      %li
        = "#{notification.dude} #{notification.did_what}"
        = link_to notification.excerpt, notification.able.classify.constantize.find(notification.able_id)